kind = "web"
previewPath = "/"
title = "AI Dietitian"
version = "1.0.0"

[[services]]
localPort = 8000
name = "AI Dietitian"
paths = ["/"]

[services.development]
run = "cd /home/runner/workspace/artifacts/dietitian && uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
